package ch.heigvd.dai.game;

public class Game {
    private int nbPlayers;
    private int nbCards;
    private int[] stackOfCards;

    public Game(int nbPlayer, int nbCards) {
        this.nbPlayers = nbPlayer;
        this.nbCards = nbCards;
    }

    // Contient les règles pour définir les méthodes du jeu

    // Distribuer cartes(nombreCartes)
    // JouerUneCarte(positionCarteDansDeck)
    // VerifierSiJeuTermine()
}
